---
// pages/admin/index.astro
// Dashboard principal do admin — redireciona para /admin/pricing que é o mais útil.
import { SESSION_COOKIE, verifyToken } from '../../utils/auth';
const token = Astro.cookies.get(SESSION_COOKIE)?.value || '';
const valid = await verifyToken(token);
if (!valid) return Astro.redirect('/admin/login');
return Astro.redirect('/admin/hero');
---
